# euroloto configuration
# Run euroloto.build_tirage() once to create tirage.xlsx from FDJ archives.
# Then use euroloto.update_tirage() to append new draws.

file: tirage.xlsx    # single file, two sheets

loto:
  sheet: loto

euro:
  sheet: euromillion
